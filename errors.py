"""
Error handlers - DEPRECATED
These are now in app.py

Error handlers are now defined directly in app.py:
    @app.errorhandler(404)
    @app.errorhandler(500)

This file is kept for reference only
"""

# Error handling code has been moved to app.py to simplify the structure
